{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([matterport-api.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'matterport-api.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
